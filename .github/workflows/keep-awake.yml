name: Keep Streamlit App Awake

on:
  schedule:
    - cron: "*/15 * * * *"   # every 15 minutes
  workflow_dispatch:          # allows manual run too

jobs:
  curl-ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Ping
        run: |
          curl -s https://pricespy-jx857shv65uogjq44k8vj3.streamlit.app > /dev/null
